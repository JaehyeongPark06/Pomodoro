import styled from "styled-components";

const _apply_styled = styled.input`
  width: 140px;
  height: 53px;
  margin-top: 36px;
  padding: 0;
  border-radius: 26.5px;
  background-color: var(--default-accent);
  font-family: Kumbh Sans;
  font-weight: bold;
  font-size: 16px;
  color: #ffffff;
  transition: transform 0.15s ease-in-out;
  cursor: pointer;
`;

export default _apply_styled;
